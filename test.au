# Not Rust lul

extern sin(x) f64;
extern cos(x) f64;
extern putf(x);

fn test_func(x, y) f64 { 
    let e;
    let z = x + y;
    e = sin(x) + cos(y);
    return e + sin(z);
}

fn main() i64 {
    putf(test_func(1.0, 2.0));
    return 0;
}