# Not Rust lul

extern sin(rad: f64) f64;
extern cos(rad: f64) f64;
extern putf(f: f64);

fn test_func(x: f64, y: f64, k: i64) f64 { 
    let e;
    let z = x + y;
    e = sin(x) + cos(y);
    return e + sin(z);
}

fn main() i64 {
    putf(test_func(1.0, 2.0, 5));
    return 0;
}